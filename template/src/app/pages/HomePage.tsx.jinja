{% if use_azure_auth -%}
import { useIsAuthenticated } from '@azure/msal-react'
import { getCurrentUser, logout } from '../../auth/msalConfig'
{%- endif %}

export function HomePage() {
  {% if use_azure_auth -%}
  const isAuthenticated = useIsAuthenticated()
  const user = getCurrentUser()
  
  const handleLogout = async () => {
    try {
      await logout()
    } catch (error) {
      console.error('Logout failed:', error)
    }
  }
  {%- endif %}

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      {/* Header */}
      <header className="bg-white shadow-sm">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-3">
              <img 
                src="/alkeme-logo.svg" 
                alt="ALKEME Insurance" 
                className="h-12 w-auto"
              />
              <div className="border-l border-gray-300 pl-3">
                <p className="text-sm text-gray-600 italic">Insurance everything.</p>
              </div>
            </div>
            
            {% if use_azure_auth -%}
            {isAuthenticated && user && (
              <div className="flex items-center gap-4">
                <span className="text-sm text-gray-600">
                  {user.name || user.username}
                </span>
                <button
                  onClick={handleLogout}
                  className="px-4 py-2 text-sm font-medium text-white bg-red-600 rounded-md hover:bg-red-700"
                >
                  Logout
                </button>
              </div>
            )}
            {%- endif %}
          </div>
        </div>
      </header>

      {/* Main Content */}
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div className="text-center mb-12">
          <h2 className="text-4xl font-extrabold text-gray-900 sm:text-5xl">
            Shaping the future of insurance
          </h2>
          <p className="mt-4 text-xl text-gray-600">
            More than just an insurance agency, we're a single solution for all of your needs.
          </p>
        </div>

        {/* Feature Cards */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <div className="p-6 bg-white rounded-lg shadow-md">
            <h3 className="text-xl font-semibold text-gray-900 mb-2">
              🚀 Innovative
            </h3>
            <p className="text-gray-600">
              Changing how insurance companies operate, market, grow and succeed
            </p>
          </div>
          
          <div className="p-6 bg-white rounded-lg shadow-md">
            <h3 className="text-xl font-semibold text-gray-900 mb-2">
              🤝 Authentic
            </h3>
            <p className="text-gray-600">
              Trusted leadership in insurance and financial services since 2020
            </p>
          </div>
          
          <div className="p-6 bg-white rounded-lg shadow-md">
            <h3 className="text-xl font-semibold text-gray-900 mb-2">
              💡 Creative
            </h3>
            <p className="text-gray-600">
              Taking unique approaches to solving problems in the insurance industry
            </p>
          </div>
        </div>

        {/* Environment Info */}
        <div className="mt-12 p-6 bg-white rounded-lg shadow-md">
          <h3 className="text-lg font-semibold text-gray-900 mb-4">
            🔧 Environment Configuration
          </h3>
          <dl className="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
            <div>
              <dt className="font-medium text-gray-700">API Base URL:</dt>
              <dd className="text-gray-600 mt-1 font-mono text-xs">
                {(window as any).__ENV__?.APP_API_BASE_URL || 'Not configured'}
              </dd>
            </div>
            <div>
              <dt className="font-medium text-gray-700">Environment:</dt>
              <dd className="text-gray-600 mt-1 font-mono text-xs">
                {(window as any).__ENV__?.APP_ENVIRONMENT || 'development'}
              </dd>
            </div>
            {% if use_azure_auth -%}
            <div>
              <dt className="font-medium text-gray-700">Azure Client ID:</dt>
              <dd className="text-gray-600 mt-1 font-mono text-xs">
                {(window as any).__ENV__?.APP_AZURE_CLIENT_ID || 'Not configured'}
              </dd>
            </div>
            <div>
              <dt className="font-medium text-gray-700">Dev No Auth:</dt>
              <dd className="text-gray-600 mt-1 font-mono text-xs">
                {(window as any).__ENV__?.APP_DEV_NO_AUTH || 'false'}
              </dd>
            </div>
            {%- endif %}
          </dl>
          <p className="mt-4 text-xs text-gray-500">
            💡 Tip: These values are injected at runtime from your .env file.
            No rebuild needed to change configuration!
          </p>
        </div>
      </main>

      {/* Footer */}
      <footer className="mt-12 py-6 text-center text-sm text-gray-600">
        <p>
          © 2024 ALKEME Insurance Smart Solutions
        </p>
        <p className="mt-1 text-xs">
          Shaping the future of insurance. One partner at a time.
        </p>
      </footer>
    </div>
  )
}

